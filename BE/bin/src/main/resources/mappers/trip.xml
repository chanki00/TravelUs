<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.DB_PASSWORD_REDACTED.trip.repository.TripRepository">
  	<select id="getSidoList" resultType="Sido">
    SELECT no, sido_code AS sidoCode, sido_name AS sidoName
    FROM sidos
    ORDER BY sido_code
  </select>
  
   <select id="getAttractionBysidoCode" resultType="com.DB_PASSWORD_REDACTED.trip.dto.Attraction" parameterType="int">
        SELECT 
            no,
            content_id AS contentId,
            title,
            content_type_id AS contentTypeId,
            area_code AS areaCode,
            si_gun_gu_code AS sigunguCode,
            first_image1 as image,
            map_level AS mapLevel,
            latitude,
            longitude,
            tel,
            addr1 as addr,
            homepage,
            overview
        FROM attractions
        WHERE area_code = #{sidoCode}
    </select>
 </mapper>
 